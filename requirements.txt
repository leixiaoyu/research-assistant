# Core dependencies
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# CLI
typer>=0.9.0
rich>=13.0.0

# HTTP & Async
aiohttp>=3.9.0
httpx>=0.25.0

# LLM APIs
anthropic>=0.18.0
google-genai>=1.0.0  # New unified SDK (replaces deprecated google-generativeai)

# PDF Processing (Phase 2)
# NOTE: Requires Python 3.14+ (marker-pdf uses modern type hints)
marker-pdf>=1.0.0  # PDF to Markdown conversion with code preservation
PyMuPDF>=1.24.0    # Fast PDF text/table extraction (import fitz)
pdfplumber>=0.11.0  # Superior table extraction fallback

# Retry & Error Handling
tenacity>=8.2.0

# Caching
diskcache==5.6.3

# Logging & Observability
structlog>=23.1.0
python-json-logger>=2.0.0

# Phase 8: Deep Research Agent (DRA)
# ADR-8.2: Full torch package for future GPU upgrade path
torch>=2.0.0
transformers>=4.30.0  # For SPECTER2 embeddings
faiss-cpu>=1.7.4  # Dense vector search
rank-bm25>=0.2.2  # Sparse keyword search
numpy>=1.24.0  # Required by FAISS and transformers

# Metrics (optional for Phase 4)
prometheus-client>=0.19.0

# Scheduling (Phase 4)
APScheduler>=3.10.0

# Health endpoints (Phase 4)
fastapi>=0.109.0
uvicorn>=0.27.0

# Development & Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
black==24.1.1
mypy>=1.19.0  # Requires 1.19+ for Python 3.14 support
flake8>=6.1.0
isort>=5.12.0

# Type stubs
types-pyyaml>=6.0.0
types-requests>=2.31.0
feedparser>=6.0.10
